# GoLink 🔗

**GoLink** — это легкий и быстрый сервис сокращения ссылок (URL Shortener), написанный на чистом Go (Golang) без использования сторонних фреймворков.

Проект реализует REST API для создания коротких алиасов, перенаправления пользователей и сбора базовой аналитики (счетчик переходов).

## 🚀 Возможности

- **Сокращение ссылок:** Создание кастомных коротких ссылок (алиасов).
- **Редирект:** Мгновенное перенаправление (HTTP 302) на оригинальный URL.
- **Персистентность:** Все данные сохраняются на диск в файл `url.json`.
- **Аналитика:** Подсчет количества переходов по каждой ссылке.
- **Валидация:** Проверка на дубликаты и пустые поля.

## 🛠 Технологический стек

- **Язык:** Go (Golang)
- **Архитектура:** REST API
- **Хранение данных:** In-Memory Map + JSON Storage
- **Зависимости:** Только стандартная библиотека (`net/http`, `encoding/json`, `os`)

## ⚙️ Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone [https://github.com/krasilovalex/go-link-shortener.git](https://github.com/krasilovalex/go-link-shortener.git)
   ```

2. **Перейдите в папку проекта:**

    ```bash
    cd go-link-shortener
    ```
3. **Запустите сервер:**
    ```bash
    go run main.go
    ```

## 🔌 API Documentation
1. **Создать короткую ссылку**
**Endpoint**: POST /create

Тело запроса (JSON):

```json
{
  "url": "[https://www.youtube.com](https://www.youtube.com)",
  "alias": "yt"
}
```
Пример вызова (cURL):

```bash
   curl -X POST http://localhost:8080/create \
   -H "Content-Type: application/json" \
   -d '{"url": "[https://google.com](https://google.com)", "alias": "goo"}'
```

2. **Переход по ссылке (Редирект)**
**Endpoint**: GET /{alias}

Просто откройте в браузере: http://localhost:8080/yt Сервер перенаправит вас на YouTube и увеличит счетчик кликов.

🔜 План развития (Roadmap)
[x] Базовая логика (Create/Redirect)

[x] Сохранение в файл

[ ] Docker контейнеризация

[ ] Concurrent safe (Mutex) для высокой нагрузки

[ ] Генерация случайных алиасов

Created by krasilovalex
